// min returns the minimum of the two arguments.
fn min($a, $b) {
	if $a < $b {
		ret($a)
	} else {
		ret($b)
	}
}

// max returns the maximum of the two arguments.
fn max($a, $b) {
	if $a < $b {
		ret($b)
	} else {
		ret($a)
	}
}
